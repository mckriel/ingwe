"use client";

/**
 * This component is kept for backward compatibility but does nothing
 * since the location preloading functionality has been simplified
 */
export default function PreloadLocations() {
  // This component is now a no-op
  return null;
}